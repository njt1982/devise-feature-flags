require "spec_helper"

describe Devise::FeatureFlags do
  it "has a version number" do
    expect(Devise::FeatureFlags::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
